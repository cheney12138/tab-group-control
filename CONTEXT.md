# Tab Group Search

标签管理扩展的领域语言:搜索切换、自动分组、归档收纳。所有功能以「分组」为锚点——没有分组身份的对象(未分组散标签)不进入归档等任何管理动作,这是本插件不发散成通用标签管理器的根本约定。

## Language

**分组 (Group)**:
Chrome tab group,拥有身份 = 组名 + 颜色。一切管理能力(归档、规则、自动折叠)只作用于有分组身份的对象;未分组散标签视为临时状态,不参与管理。
_Avoid_: 工作区(workspace)

**分组实例 (Group Instance)**:
Chrome 运行时里一个具体的 tab group 对象,由 `groupId` + 所属窗口界定,每次启动重新编号。它是「分组」的另一层:「分组」的身份(组名+颜色)跨重启稳定、可跨窗口重名,用于收起记忆/归档卡/规则键;实例只在本次会话内有效,是「关闭某组的标签」这类操作动作的作用域。同名不同窗 = 同一个身份、两个实例。
_Avoid_: 拿 groupKey(组名+颜色)当操作作用域

**管理动作 (Management Action)**:
需要对象先有分组身份才成立的动作:归档、规则、自动折叠。与之相对的**操作动作**(关闭、切换、拖拽移动)作用于屏幕上任何标签,未分组散标签与兜底组 Others 一律同等对待。边界由「是否需要组的身份」划定,与规则引擎对「自定义组/规则组」的分类无关。
_Avoid_: 用「管理」统称关闭类动作

**归档 (Archive)**:
把一个有身份的分组整体暂存起来的动作:拍归档卡 → 关闭组内全部标签(含同 URL 重复副本)。stash 语义,不是备份、不是收藏。
_Avoid_: 收纳、收藏、保存、暂存工作区

**关闭 (Close)**:
把标签直接丢弃的动作:不拍快照、不留任何持久化痕迹,只有数秒内的撤销窗口能把它救回。与「归档」的唯一区别就是有没有留下可重新打开的归档卡。属**操作动作**,未分组散标签与 Others 同样可关。
_Avoid_: 用「关闭」描述归档动作(归档会顺带关闭标签,但它的承诺是留存,不是丢弃)

**归档卡 (Archive Card)**:
归档产生的一次性快照条目:{组名, 颜色, 归档时间, 标签清单[标题/URL/favicon]}。同名并允许多张并存(以时间区分),重新打开消耗后卡片即焚。仅存在于本机(storage.local),不同步、不备份、卸载即焚。
_Avoid_: 工作区卡片、存档、stash 卡片

**重新打开 (Reopen)**:
把归档卡的标签在弹窗所在窗口全新加载并归组的动作;成功后归档卡即焚。不叫"恢复"——快照不含页面现场,这个词会许诺它做不到的事。
_Avoid_: 恢复、还原

**快照 (Snapshot)**:
归档卡内的标签清单,纯 URL 维度。不含页面现场(滚动位置、表单、会话历史),不含标签序、窗口归属、pinned 状态;同 URL 的重复副本在快照中被拍平为一条。
_Avoid_: 备份、会话

**规则组 (Rule Group)**:
由域名规则引擎创建/托管的分组,组名出现在 `groupRules` 的规则键中。规则引擎有权迁移其成员;组名不在规则里的分组(自定义组)引擎不得拆散。
_Avoid_: 自动组

**最近关闭 (Recently Closed, /r)**:
按**关闭时间**倒序的已关闭标签数据源(Chrome sessions,25 条),存的是关闭那一刻的标题/URL。与 ⌘Z(6 秒内一步抢救,还原分组)、/h(按访问时间的浏览足迹)三方分工,互不合并。重开的标签交给规则引擎自然归组,不还原关闭时的现场。
_Avoid_: 恢复历史、找回

**路径规则 (Path Rule)**:
域名规则的扩展形态,chip 写 `github.com/cheney12138` 即按"主机 + 路径纯前缀"归组。命中裁决沿用最长优先:先比主机具体度(*.域 > 裸域),同级再比路径前缀长度(有路径 > 无路径,长 > 短)。仅精确主机可挂路径,`*.` 通配规则不得挂路径;路径不支持 glob。
_Avoid_: URL 规则、路径通配

**兜底组 (Others)**:
未命中任何规则的标签的收容组(可配置关闭)。组名固定为 Others,在规则引擎语义中不属于"自定义组",引擎可自由迁入迁出。

**Chrome 已保存的标签组 (Saved Tab Group)**:
Chrome 原生功能,与本扩展的「归档」无任何关系,扩展 API 不可见、不可管理。界面与文档中绝不与「归档」混用称谓。
_Avoid_: (用于表述本扩展功能时)保存的组

**主题 (Theme)**:
一套自包含的皮肤单元,经 `data-theme` 挂载,垄断自己的命名空间。明暗是主题的自由:每套主题显式声明自己的 `color-scheme`,暗色不是义务;同主题的暗色变体做 `data-theme` 内的分支,不开新主题名。
_Avoid_: 皮肤包、样式包

**系统 Token (System Token)**:
主题与组件之间唯一的接线层:`--bg/--surface/--text/--accent/--tag-*` 等。组件 CSS 只准消费系统 token,不准出现主题判断;主题必须为**视觉** token 全量赋值,度量基座(间距/圆角/字号线)主题无关、存放于 `:root`、可覆盖无义务。
_Avoid_: CSS 变量(特指本层时用)

**主题资产 (Theme Asset)**:
一个主题独有、靠选择器覆盖层或 THEME_ASSETS 表项实现的视觉件(如墨团角标、笔锋色条、搜索框长横)。合法但必须登记:块级注释 + docs/theming.md 白名单;动布局骨架的资产是特例,基础层改动其宿主时须同步检查。
_Avoid_: 主题专属样式、皮肤补丁
